import React from 'react';
import VideoSession from '../components/VideoSession';

const VideoSessionsPage = () => {
  return (
    <VideoSession sessionId="demo-session" isTeacher={false} />
  );
};

export default VideoSessionsPage;
